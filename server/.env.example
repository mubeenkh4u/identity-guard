PORT=8787
# Create a Brave Search API key and keep it only in server/.env or your hosting provider's secret store.
BRAVE_SEARCH_API_KEY=
# Optional: enables breach checks for an email the user is authorized to audit.
HIBP_API_KEY=

# Optional client override for local/native development.
# Leave unset in the Hostinger production build so the web app uses same-origin /api.
# Android emulator example: http://10.0.2.2:8787/api
# Physical phone example: http://192.168.1.100:8787/api
EXPO_PUBLIC_API_BASE_URL=http://localhost:8787/api
